import React from 'react';

const Result = props => {
  return (
    <article>
      <section>
        <h2>{props.title}</h2>
      </section>
    </article>
  );
};

export default Result;
